# STEALTHWALL — runtime deps for middleware + block engine + dashboard
# (training-only ML deps are NOT here: see models/coldstart/requirements.txt)
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27          # TestClient transport used by the e2e suite
